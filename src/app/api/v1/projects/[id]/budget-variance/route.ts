// Thin shim — the v1 project budget-variance report lives in src/backend/api/v1/project-budget-variance.ts.
export { GET } from '@/backend/api/v1/project-budget-variance'

export const dynamic = 'force-dynamic'
