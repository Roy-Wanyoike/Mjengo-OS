# Mjengo-OS — QA Report (2026-09-10, audit-fix wave)

**Verdict: READY WITH APPROVED RISKS** — production-ready for customer onboarding in its documented posture (single-operator/Kenya sandbox rails), with three externally-gated items tracked as honest-open issues (#40, #41, #43) and one account-level action for the owner (#98). Every claim below is evidence-backed.

---

## 1. Executive summary

The full engineering organization (principal coordinator + three read-only auditors + five fix agents) executed a complete audit-fix-verify cycle on 2026-09-10: a fresh three-surface audit (backend/security, frontend/UI, website/docs/git-hygiene) of the entire codebase, 10 new GitHub issues (#102–#111) covering every finding, and 7 merged PRs (#101, #112–#117) closing all of them — including one live-verified P0 security fix (cross-project share-token read). The repository is now at its cleanest state ever: one branch, no unmerged work, no secrets in history, 1,811 passing tests across 71 files, lint and strict typecheck clean, and browser-verified golden paths in both English and Kiswahili.

## 2. Scope of this report

Backend API surface (`src/backend/**`, `src/app/api/**`), frontend workspace (`src/frontend/**`), marketing website (`mjengoos-website/**`), Prisma schema + migrations, docs set (README/ARCHITECTURE/DEPLOYMENT/CONTRIBUTING/SECURITY + docs/**), CI workflows, deployment artifacts (Dockerfile, compose, systemd), and the full git history (all refs).

## 3. Test evidence

- **Unit/integration**: `bunx vitest run` → **71 files / 1,811 tests, all passing** (baseline at wave start: 69 files / 1,700 tests; +111 tests added by this wave, each tied to an issue).
- **Lint**: `bun run lint` (eslint) → exit 0.
- **Typecheck**: `bunx tsc --noEmit` (strict) → exit 0.
- **CI (GitHub Actions)**: CANNOT RUN — the account is billing-locked (issue #98, owner action required). Per the documented evidence standard, local runs of the exact same gates (lint + tsc + vitest) are the accepted substitute; the workflows are correct and will run when unblocked. This is an honestly-disclosed limitation, not a hidden one.

## 4. Browser verification (golden paths, live app on :3000)

| Path | Result |
|---|---|
| Dev quickstart login (no NEXTAUTH_SECRET — issue #94 scenario) | Sign-in succeeds AND guarded APIs authorize (`GET /api/projects` 200) — previously 401 |
| Contractor workspace | All 13 tabs render with live data; zero console errors |
| Kiswahili locale | Login screen, Money (Pesa), Evidence (Uthibitisho), supplier portal fully SW — incl. the exact strings the audit flagged ("Milestones — money tied to proof of work" → "Hatua — pesaziunganishwa na uthibitisho wa kazi") |
| Supplier portal | Scoped surface loads; retry/settings/sign-out at 44px; reads correctly scoped (`/api/search` 403, `/api/notifications` 200 served-projects-only, `/api/jobs/run` 403) |
| Share-token binding (P0 fix) | `?share=<tok-A>&projectId=<B>` → 404; `?share=<tok-A>` → 200 correct project; **zero token echo** in public payloads |
| Project switch, create-project dialog, expense dialog, notifications, ⌘K palette | Verified during audit (2-b) and spot-verified post-fix |

## 5. Security findings — all closed

| ID | Severity | Finding | Disposition |
|---|---|---|---|
| BE-1 | **P0** | Share token not bound to project — cross-project read + token harvesting | PR #112, 9 regression tests, live-verified |
| BE-2/BE-7 | P1 | Wallet/money family without finance-role gates; hardcoded ledger actors | PR #115, service-layer gates + real-actor attribution, 18 tests |
| BE-3/BE-6 | P1 | Supplier global reads on 3 routes; replay bypass of client pin | PR #113, 17 tests |
| BE-4/BE-5/BE-8 | P2 | Uncapped bodies (ussd, AI routes); unthrottled project list | PR #114/#115, 53+ tests |
| BE-9 | P3 | USSD PIN brute-force | PR #114: per-PIN lockout (5/15min) + phone-tail fallback dropped when secret set |
| Secrets in git history | — | Full-history scan (all refs) | **CLEAN** — no rewrite needed |

## 6. Prior fixes verified still holding (regression-checked)

#73 migration drift (migrations↔schema↔live-DB: no difference), #74 boot secret fail-closed, #75 wallet idempotency (natural keys + replay-before-balance), #76 body caps + webhook warnings + job timeouts, #77 notification scope (POST; GET now scoped by #113), #94 dev fallback secret (golden tests), S1 sync flag gate, legacy ai.ts call cap, PII scrub wiring, three-way match, Daraja callback/reconcile invariants, offline app shell + SW, Kiswahili field surface (718 keys — now 2,049), render-error recovery, stale-data guard, honest server errors, a11y pins (contrast/44px/tablist).

## 7. Frontend & UI

All 13 tab bodies + core dialogs + client-facing strings render in both EN and SW (2,049 key pairs, compile-time parity gate + per-surface wiring pins). Dialog validation errors are announced (aria-describedby); client preview uses the client tab strip; supplier portal at 44px targets. Known-intentional EN: dispatch/audit labels (audit data), generated PDF document content (document artifact), USSD tab (bilingual by design).

## 8. Marketing website & docs

19 routes render; sitemap/robots/metadata compose correctly; contact form has a real, hardened endpoint (validation, honeypot, rate limit, Origin gate, 16KB cap) with retrieval now documented (DEPLOYMENT §6.3). Honesty sweeps hold (no traction claims; AI marked pilot; simulated rails labelled). Docs drift (9 items incl. test counts, CI table, build args) corrected; PRODUCT-BLUEPRINT linked with "vision — README wins" caveat; GITHUB-HANDOFF marked superseded.

## 9. Repository hygiene

One branch (`main`) on origin — 64 stale branches verified content-superseded (git-cherry sweep, zero unique work) and deleted; local tree mirrors; worktrees removed; fork remote and pull-refs pruned. No unpushed commits, no deleted features lost (zero deletions across all refs), ImgBot PR #101 merged after byte-level logo verification. The one destructive local-only commit found at session start (97792e0) was identified, verified redundant, and discarded — never pushed.

## 10. Remaining risks (approved, tracked, honest-open)

1. **#98 — CI billing lock** (owner action): Actions cannot run; local gates substitute. *Onboarding impact: none at runtime; diligence optics only.*
2. **#40 — USSD production telco gateway** (external deal): faithful `*384#` simulation; demo posture documented.
3. **#43 — M-Pesa production certification** (external): Daraja sandbox behind the seam; money never invented to look tidy (reconcile sweep).
4. **#41 — Native app deferred** (ADR-0001): PWA-first by decision, revisit triggers documented.
5. **Single-process limitation**: the LX- ledger ref counter is single-process (documented in code) — fine at current scale, noted for growth.

## 11. Go/No-Go

**GO** for customer onboarding in the documented posture. The three external items are business milestones, not engineering defects; every engineering surface audited (backend, API, frontend, UI, website, docs, history) is either green or honestly disclosed with a tracked issue.

---

### Appendix A — Issue → PR → Evidence map (this wave)

| Issue | Finding | PR | Tests added |
|---|---|---|---|
| #102 | BE-1 share-token P0 | #112 | 9 |
| #103 | BE-2/BE-7 money gates + actors | #115 | 19 |
| #104 | BE-3/BE-6 supplier scope + replay pin | #113 | 17 |
| #105 | BE-4/5/8 caps + throttle | #114 + #115 | 53 (+BE-8 in #115) |
| #106 | BE-9 USSD PIN lockout | #114 | (in the 53) |
| #107 | FE-1 Kiswahili completion | #117 | +wiring pins |
| #108 | FE-2..6 XS polish | #117 | +a11y pins |
| #109 | WD docs drift (9) | #116 | — |
| #110 | WD-8 leads retrieval | #116 | — |
| #111 | GH-1..3 branch purge + ImgBot | #101 merged + purge evidence | — |

### Appendix B — Reproduction commands

`bunx vitest run` · `bun run lint` · `bunx tsc --noEmit` · `bunx prisma migrate diff --from-migrations prisma/migrations --to-schema-datamodel prisma/schema.prisma --shadow-database-url file:/tmp/shadow.db` · `git ls-remote --heads origin | wc -l` (= 1) · live probes documented in §4.
